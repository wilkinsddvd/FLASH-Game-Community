# FLASH-Game-Community 用户信息模块需求文档（UID 体系版）

**文档版本：** v1.0
**所属模块：** 用户中心 — 用户信息 / 个人空间  
**参考对象：** Bilibili（哔哩哔哩）网页端用户信息体系  
**后端框架：** FastAPI + MySQL + Redis 

---

## 一、设计原则

1. **UID 唯一标识**：弃用数据库自增 `id` 作为对外标识，采用全局唯一的 `uid`，参考 B 站 UID 体系
    
2. **用户自主编辑**：昵称、头像、签名、性别、生日等基础信息用户可自行修改
    
3. **修改次数限制**：部分敏感字段（如昵称）限制修改频率，防止滥用
    
4. **个人空间公开化**：用户空间可被其他用户访问，展示公开信息
    

---

## 二、UID 体系设计

### 2.1 为什么不用自增 ID

|对比项|自增 ID|UID|
|:--|:-:|:-:|
|**可预测性**|连续递增，易被遍历爬取|随机/算法生成，不可预测|
|**安全性**|`id=1` 暴露系统早期用户|无规律，保护用户隐私|
|**分布式扩展**|分库分表时冲突|全局唯一，天然支持分布式|
|**用户体验**|数字太小显得平台不成熟|类似 B 站 8~10 位数字，有辨识度|
|**URL 美观**|`/user/1`|`/space/1008612345`|

### 2.2 UID 生成规则

```plain
格式：纯数字，8~10 位
范围：10000000 ~ 9999999999
生成方式：雪花算法（Snowflake）或数据库号段分配
示例：1008612345、1145141919、9527000001
```

**生成策略（推荐雪花算法）：**

```plain
41位时间戳 | 10位机器ID | 12位序列号
```

- 时间戳部分保证趋势递增
    
- 机器 ID 支持分布式部署
    
- 序列号保证同一毫秒内不冲突
    
- 最终转为 10 进制数字字符串存储
    

### 2.3 UID 

表格

| 字段    | 类型                     | 用途                             |
| :---- | :--------------------- | :----------------------------- |
| `uid` | BIGINT UNSIGNED UNIQUE | **对外唯一标识**，所有 API、URL、展示均使用此字段 |


**对外规则**：

- API 路径：`/api/users/{uid}`
    
- 个人空间 URL：`/space/{uid}`
    
- 帖子作者信息：返回 `uid` + `nickname` + `avatar`


---

## 三、用户信息字段


### 3.1 用户可编辑字段清单

表格

| 字段            |   默认值   | 是否必填 |    修改限制     | 说明             |
| :------------ | :-----: | :--: | :---------: | :------------- |
| `nickname`    |    空    |  否   | 90 天内限改 1 次 | 显示昵称，2~20 字符   |
| `avatar`      |  默认头像   |  否   | 30 天内限改 5 次 | JPG/PNG，最大 2MB |
| `bio`         |    空    |  否   |     无限制     | 个人签名，0~30 字符   |
| `gender`      |  0（保密）  |  否   |   选择后无法修改   | 单选：保密/男/女      |
| `birthday`    |  NULL   |  否   |   选择后无法修改   | 日期选择器          |
| `location`    |    空    |  否   |     无限制     | 省/市级联选择        |
| `space_cover` |    空    |  否   |     无限制     | 空间背景图，最大 5MB   |
| `space_theme` | default |  否   |     无限制     | 主题切换           |

### 3.3 修改受限字段

表格

| 字段         | 修改限制         | 说明                 |
| :--------- | :----------- | :----------------- |
| `username` | 180 天内限改 1 次 | 登录用户名，修改后需重新登录     |
| `nickname` | 90 天内限改 1 次  | 参考 B 站，防止频繁改名骚扰/诈骗 |
| `uid`      | **永久不可修改**   | 全局唯一标识，生成后固定       |
| `email`    | 需验证新邮箱       | 修改后需重新验证           |


---

## 四、接口清单

### 4.1 获取当前登录用户信息

plain

```plain
GET /api/users/me
```

#### 响应示例

JSON

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "uid": 1008612345,
    "username": "wilkins_ddvd",
    "nickname": "游戏老王",
    "avatar": "https://cdn.flash-game.com/avatars/1008612345.jpg",
    "bio": "专注魂系游戏攻略，欢迎交流",
    "gender": 1,
    "birthday": "1995-08-15",
    "location": "广东/深圳",
    "space_cover": "https://cdn.flash-game.com/covers/1008612345.jpg",
    "space_theme": "dark",
    "level": 5,
    "exp": 1250,
    "post_count": 42,
    "reply_count": 128,
    "like_received": 356,
    "follower_count": 89,
    "following_count": 34,
    "role": "user",
    "nickname_can_change_at": "2026-10-28T00:00:00+08:00",
    "created_at": "2025-03-15T10:00:00+08:00"
  }
}
```

---

### 4.2 获取指定用户公开信息（个人空间）

plain

```plain
GET /api/users/{uid}
```

#### 响应示例

JSON

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "uid": 1008612345,
    "nickname": "游戏老王",
    "avatar": "https://cdn.flash-game.com/avatars/1008612345.jpg",
    "bio": "专注魂系游戏攻略，欢迎交流",
    "gender": 1,
    "birthday": "1995-08-15",
    "location": "广东/深圳",
    "space_cover": "https://cdn.flash-game.com/covers/1008612345.jpg",
    "space_theme": "dark",
    "level": 5,
    "exp": 1250,
    "post_count": 42,
    "reply_count": 128,
    "like_received": 356,
    "follower_count": 89,
    "following_count": 34,
    "role": "user",
    "created_at": "2025-03-15T10:00:00+08:00"
  }
}
```

#### 业务规则

- 返回的是**公开信息**，不包含邮箱、手机号等敏感字段
    
- 若用户被禁用（`status=0`），返回 404
    
- 游客（未登录）也可访问此接口
    

---

### 4.3 更新当前用户信息

plain

```plain
PUT /api/users/me
```

#### 请求体

JSON

```json
{
  "nickname": "新的昵称",
  "bio": "新的个人签名",
  "gender": 2,
  "birthday": "1996-01-01",
  "location": "北京/海淀",
  "space_theme": "blue"
}
```

#### 字段校验规则

表格

| 字段            | 规则                            |
| :------------ | :---------------------------- |
| `nickname`    | 1~20 字符，支持中文/英文/数字/下划线，不允许纯数字 |
| `bio`         | 0~30字符                        |
| `gender`      | 0/1/2                         |
| `birthday`    | 有效日期                          |
| `location`    | 省/市格式，从预设选项中选择                |
| `space_theme` | `default` / `dark` / `blue`   |

#### 响应示例

JSON

```json
{
  "code": 200,
  "message": "更新成功",
  "data": {
    "uid": 1008612345,
    "nickname": "新的昵称",
    "bio": "新的个人签名",
    "updated_at": "2026-07-29T01:30:00+08:00"
  }
}
```

#### 错误响应

JSON

```json
{
  "code": 400,
  "message": "昵称修改过于频繁",
  "data": {
    "field": "nickname",
    "can_change_at": "2026-10-28T00:00:00+08:00",
    "remaining_days": 90
  }
}
```

---

### 4.4 上传头像

plain

```plain
POST /api/users/me/avatar
```

#### 请求体

`multipart/form-data`

表格

|字段|类型|说明|
|:--|:--|:--|
|`file`|File|头像图片，JPG/PNG，最大 2MB|

#### 响应示例

JSON

```json
{
  "code": 200,
  "message": "上传成功",
  "data": {
    "avatar": "https://cdn.flash-game.com/avatars/1008612345_20260729.jpg"
  }
}
```

#### 业务规则

- 图片自动压缩至 300x300 像素
    
- 上传后更新 `users.avatar` 字段
    
- 旧头像文件立即删除

---

### 4.5 上传空间背景图

plain

```plain
POST /api/users/me/space-cover
```

#### 请求体

`multipart/form-data`

表格

|字段|类型|说明|
|:--|:--|:--|
|`file`|File|背景图，JPG/PNG，最大 5MB|

#### 响应示例

JSON

```json
{
  "code": 200,
  "message": "上传成功",
  "data": {
    "space_cover": "https://cdn.flash-game.com/covers/1008612345_20260729.jpg"
  }
}
```

---

### 4.6 修改用户名（受限）

plain

```plain
PUT /api/users/me/username
```

#### 请求体

JSON

```json
{
  "username": "new_username",
  "password": "当前密码"
}
```

#### 业务规则

- 需验证当前密码
    
- 180 天内限改 1 次
    
- 用户名 3~20 字符，字母/数字/下划线，唯一

---

### 4.7 检查昵称/用户名是否可用

plain

```plain
GET /api/users/check-availability
```

#### 查询参数

表格

|参数|类型|必填|说明|
|:--|:--|:-:|:--|
|`field`|string|✅|`nickname` 或 `username`|
|`value`|string|✅|要检查的值|

#### 响应示例

JSON

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "available": false,
    "reason": "该昵称已被占用"
  }
}
```

---

## 五、个人空间页面（前端参考 B 站）

### 5.1 页面结构

plain

```plain
┌─────────────────────────────────────────────┐
│  空间背景图（全宽，用户可自定义上传）            │
├─────────────────────────────────────────────┤
│  头像（圆形，叠加在背景图下方）                 │
│  昵称 + 等级徽章 + 角色标识                    │
│  UID: 1008612345  （可复制）                  │
│  签名/简介                                    │
│  关注 | 粉丝 | 获赞                           │
├─────────────────────────────────────────────┤
│  Tab 导航：动态 | 投稿 | 收藏 | 回复          │
├─────────────────────────────────────────────┤
│  内容区（帖子列表 / 回复列表 / 收藏列表）       │
└─────────────────────────────────────────────┘
```

### 5.2 B 站风格参考点

表格

|元素|B 站实现|FLASH 实现|
|:--|:--|:--|
|**UID 展示**|个人空间显著位置显示 UID，可复制|空间头部显示 `UID: 1008612345`，点击复制|
|**等级系统**|Lv1~Lv6 彩色徽章|Lv1~Lv10，根据经验值自动计算|
|**空间背景**|可上传自定义背景图|支持上传，默认使用系统预设背景|
|**主题切换**|深色/浅色模式|`default` / `dark` / `blue`|
|**数据统计**|关注、粉丝、获赞醒目展示|同样展示，数据从 users 表读取|
|**内容 Tab**|投稿、收藏、动态分区|帖子、回复、收藏分区|

---

## 六、用户等级系统（参考 B 站）

### 6.1 等级规则

表格

|等级|所需经验|标识|
|:-:|:-:|:--|
|Lv1|0|灰色|
|Lv2|200|绿色|
|Lv3|1500|蓝色|
|Lv4|4500|紫色|
|Lv5|10800|橙色|
|Lv6|28800|红色|

### 6.2 经验值获取

表格

| 行为    | 经验值  |
| :---- | :--: |
| 每日登录  | +10  |
| 发帖    | +20  |
| 回复    | +10  |
| 被点赞   |  +5  |


### 6.3 接口

plain

```plain
GET /api/users/{uid}/level
```

JSON

```json
{
  "code": 200,
  "data": {
    "level": 5,
    "current_exp": 1250,
    "next_level_exp": 28800,
    "progress_percent": 4.3,
    "title": "资深玩家"
  }
}
```


---

## 七、Redis 缓存设计

表格

| Key                        | 类型     | TTL   | 说明           |
| :------------------------- | :----- | :---- | :----------- |
| `user:{uid}:profile`       | Hash   | 600s  | 用户公开信息缓存     |
| `user:{uid}:me`            | Hash   | 300s  | 当前用户私有信息缓存   |
| `user:username:{username}` | String | 3600s | 用户名 → UID 映射 |
| `user:nickname:{nickname}` | String | 300s  | 昵称查重缓存       |
|                            |        |       |              |

---

## 八、验收标准

表格

| 验收项    | 通过标准                             |
| :----- | :------------------------------- |
| UID 生成 | 注册用户时自动分配 8~10 位数字 UID，全局唯一      |
| 对外标识   | 所有 API、URL、展示均使用 UID，自增 id 不对外暴露 |
| 信息编辑   | 用户可修改昵称、头像、签名、性别、生日、所在地、空间背景     |
| 修改限制   | 昵称 90 天内限改 1 次，用户名 180 天内限改 1 次  |
| 个人空间   | 可通过 `/space/{uid}` 访问任意用户的公开信息   |
| 等级系统   | 经验值自动计算，等级正确展示                   |

---
