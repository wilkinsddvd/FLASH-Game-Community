# 2026-08-04

## Squad 编制模块数据修复（用户反馈 bug）

### 问题
静态展示系统「Squad编制」存在：阵营缺失、编制信息失实、票数失实、坦克缺失、单兵武器缺失。

### 修复内容（commit cb922af）
1. **阵营补齐到 17 个**：USMC/USA/RGF(原RUS)/VDV/BAF(原UK)/CAF/PLA/PLAAGF/PLANMC/ADF/AFU/TLF/GFI(原MEA→伊朗)/IMF(塞尔维亚民兵)/MEI(原INS→中东叛军)/CRF/WPMC
2. **编制类型补齐到 7 种**：新增 `airassault` 空降编制（BG_TYPE_LABELS + NATO_ICON 增加伞降图标）
   - 保留游戏设定空缺：IMF/MEI 无空降，PLA 无机械化
3. **坦克补全**：USA 新增装甲旅(M1A2 Abrams)、VDV(Sprut-SDM1/T-72B3)、PLAAGF(ZTD-05/ZTZ-99A)、AFU(T-64BM2)、TLF(M60T)、GFI(T-72S)、MEI(T-62)、WPMC(M60T) 等
4. **新增单兵武器字段**：每阵营 `soldier_weapons`（步枪手/自动步枪手/精确射手/机枪手等），SquadRosterPage 新增「单兵武器」区块+锚点
5. **票数修正**：按 squad.fandom.com Wiki 抓取数据校对（MBT=15/1200s、IFV=10、APC=5-10、卡车6-8、直升机5 等）

### 数据源
- wiki 抓取：squad.fandom.com api.php（AFU/TLF/VDV/WPMC/CRF/GFI/PLAAGF/PLANMC/MEI 页面）
- 遗留 /tmp/squad_vehicle_stats.json 等

### 注意
- 远程默认分支是 main，但历史提交在 master；本次已同时推送 master 和 main（cb922af）
- 新阵营旗帜多为 SVG 占位（无真实 PNG），后续可替换 flagcdn 资源

## 加载态 + 赞助页（commit 5ea5af6）

### 加载态（用户体验优化）
- 全局路由切换顶部进度条：`route-progress`（App.vue + style.css），懒加载页面切换时显示，避免白屏
- 页面切换淡入过渡：`<transition name="page-fade" mode="out-in">` 包裹 RouterView
- 首页数据区骨架屏：banner/news/guides 加载中显示 `.skeleton` 动画占位（HomePage.vue + style.css 通用类）

### 赞助功能
- 新增静态页 `SponsorPage.vue`（/sponsor）：赞助说明 / 付款方式占位（微信/支付宝/爱发电）/ 三档赞助 / 赞助名单（空占位）
- 首页新增「赞助支持」卡片栏（sponsor-card），引导至 /sponsor
- 导航栏新增「赞助」入口；router.js 注册路由

### 待办
- 赞助页付款码 qr 为空，需替换为真实收款码/链接
- 赞助名单当前为空数组，后续可从后端接入
