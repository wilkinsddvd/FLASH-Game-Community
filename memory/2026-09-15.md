# 2026-09-15

（承接 09-14 深夜的连续工作）

## 01:11~01:30 上云部署（squadflash.cn）—— 已完成主体，卡在备案

### 用户指令
1. 已在备案中，**不再用 wanglinhao.cn**；2. 新增 squadflash.cn；3. 云库清测试账号 + 把 squad 编制/基础认证数据传到云库；4. 镜像名：后端 `squad-flash-end`、前端 `squad-flash-fronted`（用户原文缺个 n，按原样用）。

### 关键发现（云服务器 39.96.182.141）
- **`squadflash.cn` + `www` 已解析到服务器**（阿里云 DNS），但**被阿里云未备案拦截**：80 返 `403 Non-compliance ICP Filing`，443 直接连不上 → **HTTP-01 签证书不可行，域名暂时完全用不了**。等备案下来再签。
- 服务器：Ubuntu 22.04.5、2C/1.6G、MySQL 8.0.46（root@'%' 存在但**没授权**给 flash_game_community 库）、Redis 6.0.16（requirepass，bind 127.0.0.1/::1/172.17.0.1）、Docker 29.1.3、certbot 1.21.0（只有 nginx 插件）、**上欠 377 天**且 `reboot-required` + 15 个待升级包。
- 本地 MySQL 是 **9.3.0**（跨大版本，不整库 dump，改成“云库自动建表 + 按表导入”）。
- 本地口令表状态：admin `use_count=3`（**已用满失效**）、super `use_count=2`（只剩 1）→ **口令表绝不能迁**，靠云库种子（use_count=0 + max 3 = 各剩 3 次）。

### 执行的改动
**本地**
- 前端静态资源改为**同源相对路径**：`api.js` 新增 `export const STATIC_BASE = VITE_STATIC_BASE_URL ?? (DEV ? 'http://localhost:8000' : '')`，6 个页面（CertPage/QuizResultPage/ProfileSettings/SpacePage/QuizManage/AuditManage）去掉 `|| 'http://localhost:8000'` 兜底 → 以后换域名/上 HTTPS 不用重新构建，也不会有混合内容。
- **`requirements.txt` 补 `redis>=5.0.0`**（容器里 `ModuleNotFoundError: No module named 'redis'`；本地环境有装所以一直没暴露）。
- **`frontend/Dockerfile` 加 `RUN chmod -R a+rX /usr/share/nginx/html`**：仓库里图片是 600，COPY 后 nginx worker 读不了 → favicon / squad-assets / sponsor-qr 全 403。
- 构建（buildx `--platform linux/amd64`）：`squad-flash-end:latest`、`squad-flash-fronted:latest`；save+gzip+scp+load。

**服务器**
- nginx：全目录备份 `/root/nginx-backup-20260915.tgz`；新增 `/etc/nginx/sites-available/squadflash.cn`（软链到 sites-enabled）——**目前只有 80 段**（ACME 目录 + 301）；443 段已备好在 `/root/squad-flash/squadflash.cn.conf`（含 `/uploads` 反代 + `client_max_body_size 12m`），**等证书就位再启用**。
- `sites-available/default`（既有的 default_server :80 块）**只增不改**：加了 `location /uploads/` + `client_max_body_size 12m`（备份 `default.bak.20260915-012159`，diff 已给用户）。
- 云库：新建专用账号 **`flash_app`@'%'`**（强随机密码，只授权 `flash_game_community.*`；密码存服务器 `/root/squad-flash/db_app_password.txt`，0600）。
- 容器（均 `-p 127.0.0.1:` 只绑回环 + `--restart unless-stopped` + `-v /etc/localtime:/etc/localtime:ro` + 日志上限）：
  - `squad-flash-end-instance`（env-file `/root/squad-flash/.env`：DB_ENV=cloud、CLOUD_DATABASE_URL→`flash_app@172.17.0.1:3306`、REDIS_URL→`172.17.0.1:6379` + 服务器里的密码、**新生成 SECRET_KEY**、SMTP/EMAIL_ENCRYPT_KEY 沿用本地、TZ=Asia/Shanghai；`-v flash_uploads:/app/uploads`）
  - `squad-flash-fronted-instance`（:3000）
- 数据导入（容器内跑 `/root/squad-flash/import_flash.py`）：`quiz_questions`130 + `squad_config`1（enabled=1, 278227 字节）+ `site_likes`1；**users 保持 0**（测试账号不上云）；口令池各 1 条 use_count=0。

### 验证结果
- 容器内：`/health` 200、`/api/squad/public` 200（有编制数据）、`/api/quiz/categories` 200、`/api/badges` 200、验证码接口出图（Redis 通）
- 经外层 nginx：`/`→前端 200、`/api/*`→后端 200、`/uploads/x.jpg`→后端 404（说明反代正确）
- 外网（IP）：首页/JS/favicon/squad-assets/sponsor-qr/编制接口/题库接口/验证码全部 200
- 新代码生效：登录不带图形验证码 → 400「验证码错误或已过期」
- bcrypt 哈希/校验 ✓（`(trapped) error reading bcrypt version` 是 passlib+bcrypt4.x 的已知无害告警）
- 云库最终：users 0 / roles 4 / permissions 22 / badges 13 / quiz_questions 130 / squad_config 1 / site_likes 1 / 两个口令池各 1（use_count 0,0）

### 未完（等用户）
- **squadflash.cn 证书 + 443 nginx 段**：卡备案。备案通过后跑 `certbot certonly --webroot -w /var/www/html -d squadflash.cn -d www.squadflash.cn`（webroot，不改现有文件），再把 `/root/squad-flash/squadflash.cn.conf` 拷成站点配置 → `nginx -t && systemctl reload nginx`。若想现在就要证书 → 只能走 DNS-01（阿里云 TXT 或 RAM AK + certbot-dns-aliyun）。
- 小程序侧（未做）：`config/index.ts` 目前**没有生产分支**（只有模拟器/局域网 IP），需加 `https://squadflash.cn`；mp 后台配 request/downloadFile 合法域名。
- 待办建议：服务器低峰重启（内核更新）、`vue-app:latest` 旧镜像可删、`/root/squad-flash/*.tar.gz` 可删、MySQL 3306 建议 bind 收紧、上线后改初始口令、撤掉我这次的 SSH 公钥。
