# 2026-08-03

## 《战术小队》阵营编制展示模块（Squad Rosters）

### 需求
- PRD v1.0：FLASH 社区首页加「Squad 编制」栏目，纯静态展示页
- 每个页面独立展示一个阵营的一种编制：编制概览 / 载具配置表（票数、复活时间、初始延迟）/ 特装装备表 / 编制特性 / 指挥官技能 / 页面导航

### 实现（全部完成 ✅）
- 数据层：`frontend/src/data/squad/factions.js`
  - 10 阵营 30 编制（USMC/USA/RUS/UK/CAF/PLA/ADF/MEA/IMF/INS），字段严格按 PRD JSON Schema
  - SVG 旗帜（虚构阵营）+ NATO 风格编制类型图标（SVG data URI）
  - 载具真实数据：从 Squad Fandom Wiki 抓取（票数/复活时间/分类），127 条载具条目更新，分钟→秒换算
  - 抓取脚本在 /tmp（fetch_squad_data.py 等，未入库）
- 页面：
  - `SquadPage.vue`（/squad）：阵营卡片网格、搜索框、编制类型筛选
  - `SquadRosterPage.vue`（/squad/:faction/:roster）：六大模块、排序（表头循环）/筛选/自定义 Tooltip/折叠/锚点、响应式
- 路由：router.js 注册两条；App.vue 导航加「Squad编制」；HomePage 加栏目卡片
- 图片：`frontend/public/squad-assets/`（39 载具图 + 6 真实旗帜 flagcdn），构建后进 dist
- 主题修复：Squad 页面硬编码深色导致白天模式黑 UI → 全局 style.css 新增 `--sq-*` 双主题变量（:root 亮色 / [data-theme="dark"] 暗色），两页面全部改变量引用；语义警示色（票数红/橙/绿、复活分级）保留

### 提交
- `6155c5b` feat: Squad 编制模块 + Wiki 数据 + 图片资源（53 文件 +2574）
- `9eae535` fix: 亮/暗双主题适配（3 文件 +188/-128）
- 已推送 origin master

### 遗留 TODO
- 编制内载具组成是按游戏惯例推的演示数据，需按当前游戏版本（Patch Notes/编制选择界面）核对
- 少数载具无 Wiki 独立页面/图片（MTLB/JLTV/HEMTT/MTVR/M777/HMT Jackal），票数为同类型赋值
- USMC 用简化 SVG 旗（无真实国旗）；MEA/IMF/INS 为虚构阵营保留生成旗帜
- 数据版本标注 v1.0 待校对

### 备注
- Wiki 抓取要点：squad.fandom.com 页面 HTML 被 Cloudflare 拦，用 `/api.php?action=parse&prop=wikitext` 可行
- 载具页表格是「4 表头行+4 数据行」分块键值结构，按 `|-` 分块后 zip 配对解析
- 后勤车/直升机数据在 infobox 的 `ticket value`/`respawn timer` 字段
- 页面主图用 `prop=pageimages&piprop=thumbnail` 拿 URL
- flagcdn.com 可用（Wikimedia 被墙/超时）
