# 2026-09-24

## 00:36~01:00 用户提出云服务器三需求（先出方案，确认后再动手）

### 用户原话要点
- 云服务器已可终端连接；域名 squadflash.cn **ICP 备案已通过**：**鲁ICP备2025198092号-2**
- 需求：① https://squadflash.cn/ SSL 证书过期 → 更新；② 网站底部更新备案信息；③ 优化移动端适配
- 明确要求：**先提方案 → 用户确认 → 再操作**

### 侦察结果（已实测，2026-09-24 00:52）
- 服务器 39.96.182.141（阿里云，Ubuntu 22.04，nginx 1.18.0，hostname ubuntu-ddvd）；SSH 密钥登录 root 可用（BatchMode 直连 OK）
- 站点结构：宿主 nginx → `127.0.0.1:3000` 前端容器 `squad-flash-fronted`（Vue3+Vite SPA，dist 打进 nginx:alpine 镜像，**无 bind mount**，更新必须重建镜像）；`/api/`、`/uploads/` → `127.0.0.1:8000` 后端容器 `squad-flash-end` + MySQL/Redis
- **SSL 现状**
  - 443 只有一个 default_server，证书是 `CN=wanglinhao.cn`（SAN: wanglinhao.cn, www.wanglinhao.cn），**2026-09-22 08:13 UTC 过期**；squadflash.cn **没有自己的证书**，SNI 落到默认站点 → 浏览器报"过期 + 域名不匹配"
  - 自动续期一直失败：certbot 2026-09-23 17:29 日志 `no valid A records found for wanglinhao.cn`、`www.wanglinhao.cn NXDOMAIN`（09-15 起已弃用 wanglinhao.cn，DNS 已删）→ HTTP-01 永远过不去
  - 备案拦截已解除：外网可获取 `http://squadflash.cn/.well-known/acme-challenge/precheck.txt` = `challenge-ok-1789405926` → **HTTP-01 可行**
  - 09-15 备好的 443 配置在 `/root/squad-flash/squadflash.cn.conf`，但含 `http2 on;` —— **nginx 1.18 不支持**，启用前必须改成 `listen 443 ssl http2;`
- **页脚**：`frontend/src/App.vue` → `<footer class="site-footer"><p>@ 2026 squadflash</p></footer>`
- **移动端**：`src/style.css` 已有 768/480 断点（含 header 换行+导航横向滚动、admin 侧栏折叠、el-dialog 92% 变量 hack——EP 2.14 用 inline `--el-dialog-width`，author !important 可覆盖，故该 hack 生效）；另有 5 个文件有局部断点（MessageBoard/SquadPage/SquadRosterPage/HomePage + style.css）
- 本地环境：Docker 29.2.1 可用；**未装 Chromium/Playwright**（移动端截图体检需下载 ~150MB）
- 部署方式（沿用 09-15）：本地 `docker buildx build --platform linux/amd64 -t squad-flash-fronted:latest .` → `docker save | gzip` → scp → 服务器 `docker load` → 重建容器

### 待用户拍板
1. wanglinhao.cn 是否彻底退役（删旧证书 + 去掉 default 里对应 server 段 + squadflash.cn 设为 default_server）？
2. 有无公安备案号要一起加？页脚文案格式？
3. 是否允许本地装 Playwright Chromium 做移动端截图体检？

### 状态
方案已给出，**未执行任何服务器改动**。

---

## 01:36~01:50 用户确认 → 全部执行完毕

### 用户拍板
1. wanglinhao.cn **彻底退役**；2. **没有公安备案号**；3. 允许装 Playwright。

### 需求 1：SSL —— 已完成 ✅
- 备份：`/root/nginx-backup-20260924-013714.tgz`、`/root/nginx-T-20260924-013714.txt`、站点文件备份 `default.bak.20260924-013714` / `squadflash.cn.bak.20260924-013714`、续期配置备份 `wanglinhao-renewal-20260924-013714.conf`
- 签发：`certbot certonly --webroot -w /var/www/html -d squadflash.cn -d www.squadflash.cn --non-interactive --agree-tos --deploy-hook "systemctl reload nginx"` → 证书到 **2026-12-22**（renewal conf 已含 renew_hook）
- 新 `/etc/nginx/sites-available/squadflash.cn`：80(ACME+301) + **443（`listen 443 ssl http2;`，注意不是新版的 `http2 on;`）**，含 /uploads、/api、/ 三段反代 + client_max_body_size 12m
- 重写 `/etc/nginx/sites-available/default`：80 默认站（保持 IP 直连可用）+ **443 默认站改用 squadflash.cn 证书并 301 到主站**；删除两个 wanglinhao.cn server 段
- `certbot delete --cert-name wanglinhao.cn` 已删；`certbot renew --dry-run` **通过**（自动续期恢复）
- 实测：https://squadflash.cn/ 200（HTTP/2、有效证书）、www 200、http→https 301、wanglinhao.cn SNI 现给 squadflash 证书 + 301、/health 与 /api/squad/public 均 200

### 需求 2：页脚备案 —— 已完成 ✅
- `App.vue` 页脚改为 `© 2026 SquadFlash ｜ 鲁ICP备2025198092号-2`，备案号链接 `https://beian.miit.gov.cn/`（新窗口）
- `style.css` 加 `.beian-link` 样式 + ≤480px 页脚字号/内边距调整

### 需求 3：移动端 —— 已完成 ✅（先体检后改）
体检（Playwright headless Chromium，375/390/414/768 × 15 个公开路由）：**原有版本无横向溢出**，问题集中在体验细节：
- 主导航内容宽 514px，375 屏只能看到 2/3，需横向滑动且无提示
- 导航链接高 21px、按钮 32px（低于 44px 触控标准）；登录/注册输入框 13.3~14px（iOS 聚焦自动放大）
- 编制名册 `.sort-hint` 11px；名册表 640px min-width 需横滑但无提示
改动：
- `App.vue`：加汉堡开关 `.nav-toggle` + `navOpen`，导航 ≤640px 收成抽屉（绝对定位覆盖层，路由切换自动收起）
- `style.css`：≤640px 抽屉样式（导航项 49px 高）、≤768px 输入框 16px、`.el-button` min-height 36px、`-webkit-text-size-adjust`、页头/页脚 `env(safe-area-inset-*)`
- `index.html`：viewport 加 `viewport-fit=cover`
- `SquadRosterPage.vue`：表格上方加「← 左右滑动查看完整表格 →」提示（≤768px 显示）+ 表格滚动优化 + sort-hint 12px
- 复验（本地 dist 与线上各跑一遍）：开关可见/导航默认收起/点开 8 项均 49px/点击后跳转并自动收起/0 横向溢出/输入框 16px/桌面端 1280 完全不受影响

### 部署
- 本地 `docker build --platform linux/amd64 -t squad-flash-fronted:latest .` → save+gzip → scp → `docker load` → 重建容器（旧镜像已打标签 `squad-flash-fronted:prev-20260924` 可回滚）
- 线上新产物：`index-JR6oWGMv.js` / `index-oXtOCgMO.css`；已核对线上 CSS/JS 含 nav-toggle、beian-link、备案号、beian.miit.gov.cn

### 遗留/可选
- 登录后页面（认证记录表、我的反馈、站内信、个人设置、个人空间、管理后台）未做移动端体检（需账号/需在本地起后端），如需可提供测试账号或本地起服务再查
- 服务器仍 `reboot-required`、`/root/squad-flash/*.tar.gz` 可删、旧镜像 `vue-app:latest` 可删、我这次的 SSH 公钥可撤
- 未做：HSTS、www→非 www 规范化（未问用户）
