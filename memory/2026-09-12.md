# 2026-09-12

（承接 9/11 深夜的连续工作，01:31 记录）

## 项目坐标（重要）

- **主仓库**：`/Users/linhao_wang/Desktop/dev/FLASH`（网页端 `frontend/` + 后端 `FLASH_END_PRO/`，FastAPI + MySQL + Redis）
- **小程序**：`/Users/linhao_wang/WeChatProjects/FLASH-Game-Community`（`miniprogram/` 目录）——**不在任何 git 仓库里**，改它不会产生 commit；主仓库的自动提交规则管不到它。
- **后端接口调试技巧**：超管账号 user 4 默认是禁用状态。测试管理端接口时临时把它启用、生成 token 跑 curl，**测完必须还原 status=0**（两次都这么做的）。
- **数据库**：用 pymysql 直连；小程序实际展示的编制数据来自库里的 `squad_config` 覆盖数据，改它之前先备份到 `/tmp/flash_bak/`。

## 9/11 解决的两个大问题

### 1. 小程序「编制」页打不开（真正的根因）
- 现象：编制页停在 loading，一直转圈。
- **证据来源（记住这个路径）**：模拟器运行时日志
  `~/Library/Application Support/微信开发者工具/<hash>/WeappLog/...` 以及
  `.../WeappSimulator/WeappFileSystem/<appid>/usr/miniprogramLog/log1`
  → 首页/攻略页 `onLoad` 后都有 `wx.request api invoke`，**编制页一条都没有** ⇒ onLoad 在发请求前就抛异常了。
- **根因**：`project.config.json` 里 `"es6": false`（微信 TS 模板默认：ES6→ES5 转换关闭，交给 TypeScript 插件），而 **TS 插件只编译 `.ts`**。我从网页端拷来的 `data/factions.js` 是纯 `.js`，里面的 `export` 原样留在编译产物里 → `Object.keys(BG_TYPE_LABELS)` 拿到 undefined → onLoad 崩。
- **修法**：`data/factions.js` → `data/factions.ts`（加 `// @ts-nocheck`）；并给编制页/详情页加导入兜底。验证方式：用 esbuild/tsc 转译后 `require()` 检查 → 拿到 17 个阵营 / 7 项编制类型 / 8 类载具分类。
- **教训**：小程序工程里**不要留纯 `.js` 且带 `export` 的文件**；非 ts 脚本一律改名 `.ts`。

### 2. 乌军 AFU / 俄空降军 VDV 旗帜不显示
- 根因：这两面旗是网页端的内联 **SVG data URI**，而小程序 `<image>` **不支持 SVG** → 退化成显示代号文字。
- 修法：用 PIL 按原样式精确栅格化成 **480×320 PNG**（AFU 上蓝 #0057b7 / 下黄 #ffd700；VDV 蓝底 #2f7fbf + 绿条 #3c8a3c + 金色伞盖/伞绳 #ffd700），逐点取色校验。
- **三处同步**（少一处就会不一致）：① 库里 `squad_config` 覆盖数据（小程序真正展示的那份，改前已备份）② 网页静态数据 `frontend/src/data/squad/factions.js` ③ 重建 `frontend/dist`。
- 入口：`frontend/public/squad-assets/flags/{afu,vdv}.png`。

## 9/12 凌晨完成的四条需求

### A. 最新资讯纳入超管后台
- 根因：`FLASH_END_PRO/api/cms.py` 里写死 `ARTICLE_MANAGE_CATEGORY = "developer"`，创建时还强制覆盖 → "最新资讯"(news) 根本没后台入口。
- 改为可管理栏目集合 `("news","developer")`；列表支持 `?category=`；创建/更新允许指定栏目（非法 400）；新增文章详情页 `/article/:id`（`marked` 渲染 Markdown），首页「最新资讯」和「SQUAD闪电谈」卡片都能点进去读（之前点了没反应）。
- 实测：建 news → 筛选正确 → 非法栏目 400 → 改栏目生效 → 公开接口可见 → 删除 204；测完清空 DB。

### B. 防 DDoS 方案 → 只落地图片验证码
- 方案写在 **`docs/安全限流方案.md`**（四层：nginx limit_req 接入层 / 应用层 Redis 限流 / 服务韧性 / 图形验证码）。**其余部分用户未批准，状态=待确认。**
- **用户拍板：只做图片验证码；两个接口的 IP 限流"先取消"。**
- 已实现并保留：`FLASH_END_PRO/core/captcha.py`（Pillow 画 150×50 PNG，噪点+干扰线+轻微旋转，4 位，剔除 0O1IL2Z；答案存 Redis `captcha:{id}` TTL 180 秒，**一次性**）；接口 `GET /api/auth/captcha` → `{captcha_id, image(data:image/png;base64), expires_in}`；`POST /api/auth/register` 必填 `captcha_id`+`captcha_code`，**先校验证码再查用户名重名**（防止拿注册接口探测用户名是否存在）；前端 `RegisterPage.vue` 用户名 tab 有可点击刷新的验证码，失败自动刷新。
- **已按用户要求删除**：`core/ratelimit.py`、`config.py` 里的两个限流阈值项、两个接口上的 IP 限流调用（不留死代码）。教训：遗留 `rl:` 键会短暂残留，测试后要清。
- 用户偏好信号：**范围要小、不要自作主张加东西**；功能做完要实测（curl 逐场景）。

### C. 小程序「反馈」独立成页
- 首页原来的 `<like-card />` 点赞卡片移出，首页快捷入口加「💛 反馈」→ `/packageUser/feedback/index`；`packageUser/feedback/index` 成为反馈唯一入口页。

### D. 小程序四大栏重设 + 页面清理
- tabBar 改为 **首页 / 游戏编制 / 留言板 / 关于**。
- **硬约束（重要）**：**tabBar 页面必须在主包**。原来的「关于」在分包 `packageContent` 里 → 把 `packageContent/about/*` 挪到 **`pages/about/`**（目录深度相同，`../../utils/*` 不用改），并从 subPackages 移除；新画了 ⓘ 风格图标 `images/tabbar/about{,-active}.png`（81×81）。
- 「攻略」撤下 tabBar 但**页面保留**（在首页「攻略视频 → 查看更多」进入）。跳转必须同步改：`goGuide()` 由 `switchTab` 改 `navigateTo`；首页点「关于」改 `switchTab`（加了 tab 白名单判断）。**注意：switchTab 到非 tab 页、navigateTo 到 tab 页都会静默失败**，改 tabBar 后一定要全工程核对所有跳转目标。
- 首页快捷入口去掉「关于」（已是一级 tab，避免重复）：现为 反馈 / 基础认证 / SQUAD闪电谈。
- 编制页导航标题同步改为「游戏编制」。
- 「关于」页删掉 `（后台「页面管理」添加 slug 为 about 的页面后，此处内容可在后台维护）` 小字及括弧（`pages` 表为空所以一直在显示）＋清 `.cms-hint`／`fromCms`。
- 「基础认证」页删掉黄底提示 `⚠️ 第一版暂未开放答题提交（登录功能开发中）…` ＋清 `.login-hint`／`loggedIn`／`isLoggedIn` 导入。
- 「反馈」页重做：渐变圆形 💛 hero + 居中大点赞按钮/计数 ＋「📮 提交反馈 / 建议」卡片 ＋ 底部说明；`like-card` 组件**重构为纯点赞组件**（只被该页使用，删掉 `linkTo`）。
- **未处理（已问用户）**：`packageUser/feedback-submit/index` 里同款黄底提示「⚠️ 提交反馈需要登录，登录功能开发中」——用户只说了基础认证页，等确认。

## 小程序改动的自检清单（每次改完都跑）

1. 页面/组件 JSON 合法；`app.json` 里列的页面文件都齐全
2. 所有相对 import 可解析；`tsc --noEmit` 通过
3. tabBar 页面全部在主包、图标齐全
4. 全工程 `switchTab`/`navigateTo` 目标逐个核对（防静默失败）
5. 组件被删/被重构前，先确认它只被哪些页面引用

## 提交记录（主仓库）

`5fc66bf..5d476ea`（编制页修复）→ `739bd63`（旗帜 PNG）→ `fc94097`（最新资讯后台）→ `3490b49`（限流方案文档）→ `50f5978`（图片验证码）→ `79e6e9c`（取消 IP 限流）→ `9655999`/`1bfccd8`/`c2d32a3`（小程序界面调整记录）。
小程序目录的改动不在这些 commit 里。

---

## 9/12 01:31 追加：小程序两件事

### 1) 隐藏「基础认证」
- 它唯一的 UI 入口就是首页快捷入口 → 从 `pages/home/index.ts` 的 `quickEntries` 移除（现只剩 反馈 / SQUAD闪电谈）。
- 因为没有任何入口指向 `packageCert` 了，顺手从 `app.json` 的 `preloadRule` 里去掉 `packageCert`（避免首页白下载这个分包）。**页面与分包保留**，想恢复只需把快捷入口加回去。
- `onQuickTap` 的 `cert: '/packageCert/cert/index'` 映射保留（无 UI 触发，便于恢复）。

### 2) 真机调试报错 —— 已定位并修复
- **错误（开发者工具日志里的原话）**：
  `task type:upload exec error  Error: invalid file: utils/request.js, 71:28` + `SyntaxError: Unexpected token ?`
  日志位置：`~/Library/Application Support/微信开发者工具/<hash>/WeappLog/logs/2026-09-12-00-54-02-346.log`（090:54 与 01:30 各报一次）；同一时段还有 `routeTo appLaunch timeout`（后果，不是原因）。
- **根因链**：
  1. `miniprogram/utils/request.ts:81` 写了 `resolve((res.data ?? null) as T)` —— **空值合并 `??` 是 ES2020 语法**；
  2. 项目根 `tsconfig.json` 里 `"target": "ES2020"` → 开发者工具内置的 **TypeScript 4.1.2** 按 ES2020 输出，`??` 原样保留；
  3. `project.config.json` 里 `"es6": false` → 开发者工具自己的 ES6→ES5 转换（babel）是关的，没人再降级；
  4. 真机调试/预览走的是**上传链路**，其代码包校验器只支持到 **ES2018** → 撞上 `?` 直接 SyntaxError → 上传/调试失败。
  模拟器不走该校验，所以“模拟器能跑、真机不行”。
- **修复**：`tsconfig.json` 的 `"target": "ES2020"` → **`"ES2017"`**（只动这一个字段）。ES2018+ 会降级（对象展开→`Object.assign`，`??`/`?.`→三元/条件），运行时用的都是真机支持的 API。
- **验证（用开发者工具自带的 TS 4.1.2 编译两次做对照，非常硬）**：
  - 修复前：`resolve((res.data ?? null));` → 按 ES2018 解析报 `Unexpected token 70:39`，与工具报的 `71:28` 对得上；
  - 修复后：`resolve(((_a = res.data) !== null && _a !== void 0 ? _a : null));` → **23 个产物文件在 ES2018 / ES2015 两级全部解析通过**。
  - 全工程扫描：`??` 只有这 1 处，`?.` / `||=` / `&&=` / `??=` / 可选 catch / 数字分隔符 均为 0 → 没有其它同类地雷。
- 用户需做：**重新编译（必要时 清缓存→全部清除）** 后再试真机调试。
- 经验：以后小程序的“编译不过/真机报错”先看 `WeappLog/logs/*.log`；`es6:false` 的项目里，**语法兼容由 tsconfig 的 target 负责**，别只盯 project.config.json。
