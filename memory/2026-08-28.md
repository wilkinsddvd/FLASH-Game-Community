# 2026-08-28

## 修复记忆索引（方案 C：本地 llama.cpp embedding）

用户发现 memory_search 不可用（"index metadata is missing"）。诊断链：
- 索引库为空 + 配置只配了 deepseek key，而 memorySearch 默认 provider 是 openai（无 key）→ CLI 启动即报 "No API key found for provider openai"
- 决定走方案 C：本地 GGUF embedding

踩坑记录：
1. `openclaw plugins install @openclaw/llama-cpp-provider` 的 npm install 被我自己的 180s timeout SIGKILL 了 → 后台重跑 npm install 完成
2. 插件注册要用 `openclaw plugins install ... --force`（node_modules 已存在时），manifest id 是 `llama-cpp`
3. 默认模型 `ggml-org/embeddinggemma-300m-qat-q8_0-GGUF` 从 huggingface.co 下载**被墙卡死**（10 分钟 0% CPU）→ 改用 **hf-mirror.com** 手动下载 313MB 到 `~/.openclaw/models/embeddinggemma-300m-qat-Q8_0.gguf`，并设 `agents.defaults.memorySearch.local.modelPath`
4. 被 kill 的 reindex 会留孤儿进程 + 死锁文件 `openclaw-agent.sqlite.reindex-lock.sqlite` → 杀进程 + 删锁文件后重跑 `openclaw memory index --force`
5. 网关侧生效必须重启 gateway（插件安装后未加载）

最终配置：
- `agents.defaults.memorySearch.provider = local`
- `agents.defaults.memorySearch.local.modelPath = ~/.openclaw/models/embeddinggemma-300m-qat-Q8_0.gguf`
- 17 chunks 已索引，会话内 memory_search 语义搜索验证通过

经验：国内网络 HuggingFace 直连不通，node-llama-cpp 类下载一律走 hf-mirror.com。

## Squad 编制数据全量重写（按 SQUAD编制 文档）

用户指定 `/Users/linhao_wang/Desktop/dev/squad编制`（80 份 md，`类型/阵营.md` 结构）为唯一数据源，重写前端编制。

- 与之前用的 `squad编制md文档`（28 份，按具体单位分文件）不同：80 份版是完整数据源，含真实数量/票数，且有"阵营特色/船只/前哨基地部署物"等扩展内容
- 流程：解析 80 份 md（兼容两种格式：`XX:` 与 `## XX` 标题）→ 生成计划 → Python 文本手术写入 `frontend/src/data/squad/factions.js`
- 结果：54 更新 + 26 新增 + 删除 19 个文档未覆盖的旧编制 → 前端编制 73 → **80 个**，与文档一一对应
- 编制名改为文档单位名（如"机械化编制"→"第2斯崔克骑兵旅"）
- 载具图标复用现有素材（token 重叠匹配：型号去连字符、跨语言中英匹配），无参照时按名称关键词推断分类
- 踩坑：
  1. 应用脚本按 `key:` 后第一个 `[` 定位数组会把 `tactics.strengths` 误当目标 → 必须按属性名 `vehicles: [` / `commander_abilities: [` 在 roster 括号范围内精确定位
  2. 新增编制 type_icon 颜色必须用阵营 theme，不能硬编码
  3. USMC/AFU 装甲旅坦克在文档载具表与坦克表重复出现 → 写入后去重
- 提交：68d368b（重写）、7a7aff5（删旧编制）

## Squad 页面清理（03:28-03:47）

- 删除单兵武器一栏（RosterPage 区块/锚点/样式 + HomePage 武器卡片 + factions.js SOLDIER_WEAPONS）
- 删除特装装备一栏（RosterPage kits 区块/折叠 JS/样式）
- 删除黄字"数据需按当前游戏版本校对"（SquadPage data-warn + 副标题 + 页脚）
- 旗帜修复（用户给的规范）：
  - PLA 陆军：八一军旗（pla.png）不变
  - PLAAGF 两栖部队：八一军旗底（红底黄星）+ 下部**绿白条纹**
  - PLANMC 海军陆战队：八一军旗底（红底黄星）+ 下部**蓝白条纹**
  - 验证方法：node 导出 SVG → qlmanage 转 PNG → Python zlib 解码 PNG 逐像素字符画确认（模型不支持看图，此方法可"看"渲染结果）
- 提交：7100bb9（删栏目）、dafcecb（旗帜）

## 旗帜二次修复（04:00-04:50）：直接使用用户提供的图片

用户发来 10 张旗帜图片（Squad 游戏内截图/素材），要求直接替换 SVG 占位 → 采纳用户建议，不再重画 SVG：
- 图片 → `frontend/public/squad-assets/flags/*.png`，sips 统一缩到 320px 宽（原图几百 KB 太大）
- 对应关系：PLA(pla.png 覆盖) / PLAAGF(plaagf) / PLANMC(planmc) / IMF 塞尔维亚民兵/车臣(imf) / GFI 伊朗(gfi) / CRF 加拿大叛军(crf) / TLF 土耳其(tlf) / USMC(usmc) / WPMC 西方黑水(wpmc) / MEI 中东叛军(mei)
- CRF 原图左右有截图黑边，用 sips -c 677 723 --cropOffset 0 364 裁掉（旗面 x 364-1086）
- factions.js 9 个 flag_url 从 FLAGS.XXX SVG → '/squad-assets/flags/*.png'（PLA 原本就是 png 路径）
- 验证方法：自写 png2art.py（PNG 解码→字符画）确认图片内容与阵营对应，模型不支持看图
- 新增工具：tmp_work/png2art.py（字符画渲染）、tmp_work/rgbsample.py（区域 RGB 采样）——以后看图片用
- .gitignore 追加 media/（OpenClaw 媒体暂存目录不入库）
- 提交 b33d638，已推送 origin master

## 六项功能改造（05:06-05:30）

用户 6 个需求一次性改造，提交 3133e69（27 文件 +1838）：
1. **删论坛/赞助**：App.vue 导航删两项入口 + HomePage 赞助卡片；后台删板块管理（菜单/路由/删 SectionManage 两文件）
2. **基础认证**：后端 quiz 模块（QuizDoc/QuizQuestion/QuizRecord 三表）+ CertPage.vue 答题页（90分达标）+ QuizManage.vue（QA 文档 .md/.txt 上传解析 + 题目 CRUD + 统计）
3. **攻略改 B站视频**：video 模块（bili_videos 表）+ GuidePage 改视频墙 + VideoManage.vue；点击新窗口跳 bilibili.com/video/{bvid}
4. **问题反馈**：feedback 模块（feedbacks 表，roster_error/suggestion 两类）+ FeedbackPage.vue（需登录）+ FeedbackManage.vue（处理/回复/状态）
5. **勋章系统**：badge 模块（badges/user_badges 表）+ 答题≥90 分自动发「战术精英」勋章 + SpacePage 展示（公开接口 /users/{uid}/badges，他人可见）
6. seed 新增勋章定义（幂等独立于角色种子，避免角色已存在时跳过）
- 验证：后端编译✓ 新表自动建✓ API 冒烟 200✓ 前端构建✓
- 注意：后端 uvicorn --reload 自动生效，dist 不入库
