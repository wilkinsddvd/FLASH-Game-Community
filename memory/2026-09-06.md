# 2026-09-06

## personal_ky 目录规则（用户明确要求）

- `/Users/linhao_wang/Desktop/dev/personal_ky` 是用户**个人项目**目录（2026-09-06 创建，已加入 filesystem 白名单）。
- **目录下任何内容一律不上传/不提交到 git 仓库**。禁止自动 commit/push，除非用户当时明确要求。
- 现状：dev/ 和 personal_ky/ 都不是 git 仓库（与 FLASH 仓库完全独立，FLASH 的自动提交不会触及它）；若以后 personal_ky 初始化自己的 git 仓库，同样不自动操作，等用户指示。
- 操作：备份 `~/.openclaw/openclaw.json.bak.pre-personal_ky`；filesystem MCP args 现含 FLASH + personal_ky。
