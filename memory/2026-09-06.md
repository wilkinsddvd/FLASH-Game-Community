# 2026-09-06

## personal_ky 目录规则（用户明确要求）

- `/Users/linhao_wang/Desktop/dev/personal_ky` 是用户**个人项目**目录（2026-09-06 创建，已加入 filesystem 白名单）。
- **目录下任何内容一律不上传/不提交到 git 仓库**。禁止自动 commit/push，除非用户当时明确要求。
- 现状：dev/ 和 personal_ky/ 都不是 git 仓库（与 FLASH 仓库完全独立，FLASH 的自动提交不会触及它）；若以后 personal_ky 初始化自己的 git 仓库，同样不自动操作，等用户指示。
- 操作：备份 `~/.openclaw/openclaw.json.bak.pre-personal_ky`；filesystem MCP args 现含 FLASH + personal_ky。

## 药学考研信息手册任务（晚上）

- 用户要求：调研药学考研信息，输出到 `personal_ky/药学考研.md`（2026 批次，含 A/B 区院校、2026 分数线、科目代码、按本科层次择校建议）。
- 已交付：`/Users/linhao_wang/Desktop/dev/personal_ky/药学考研.md`（八节+附录，约 500 行），含：A/B 区界定、2026 国家线（医学 A294/B284）、近 6 年趋势、第四轮药学学科评估全榜单（官方）、A 区 20 省+B 区 10 省设点院校清单（分可信级标注 ✅/◐/待核实）、34 所自划线 2026 药学相关校线（上交已官网核对）、23 校初试科目代码表（349 专硕通用、学硕代码因校而异）、梯度择校与时间轴。
- 经验教训：
  1) 本机 web_search 无 provider 且 DuckDuckGo 被墙限流；可用通道=ClashX 代理(127.0.0.1:7890)+DDG html 端点（但会限流需间隔）；搜狗走代理首次可用但不稳定；百度/360/必应国内版均反爬。工具脚本在 `/tmp/ky_search.py`（勿删，可能复用）。
  2) 已在 openclaw.json 给 tools.web.search.provider 配置 duckduckgo（本机网络不通，实际不可用；待用户提供可用的搜索 API key 或保持现状用 exec+代理）。
  3) 子代理在无可用搜索时会空转很久（本次 26-31 分钟），应及时发“停止搜索直接输出”指令止损。
  4) gateway 重启会 dispose 当前会话的 MCP runtime，下次工具调用需重试/新消息后恢复。
