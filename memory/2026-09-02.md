# 2026-09-02 工作日志

## 七项功能改造（FLASH 项目）

用户凌晨发来 7 项需求，全部完成并验证。

### 1. 邮箱登录改为验证码登录
- 后端 `api/email.py`：`send-code` 支持 `purpose`（register/login/reset）；login 验证码独立 Redis key（`verify:login:`）；`email/login` 改为邮箱+验证码，不再需要密码
- `core/email.py`：邮件文案支持登录用途
- 前端 `LoginPage.vue`：邮箱 tab 改为 邮箱 + 获取验证码（60s 倒计时）+ 验证码登录
- ⚠️ 联动修复：`ForgotPassword.vue` 原来用 `emailSendCode` 默认 purpose=register，会因"邮箱已被注册"报 409，已改为 `purpose='reset'`

### 2. 基础认证：分类专属勋章 + 答题情况页
- 新增 12 个分类勋章（quiz_rifleman…quiz_commander），`core/seed.py` + 手动补种到 badges 表
- `api/quiz.py`：达标后颁发该分类专属勋章（已拥有不重复）；新增 `GET /api/quiz/records/{id}` 返回全部题目+我的答案+正确答案+对错
- 前端：`CertPage.vue` 提交后跳转 `/cert/result/:id`；新页面 `QuizResultPage.vue`（概览+逐题对错+勋章）；记录表格加"答题情况"按钮

### 3. 超级管理员系统
- 新模型 `super_admin_passphrases`（口令池，支持增删改查），初始口令"天空那道闪电"（seed 自动写入）
- `api/super_admin.py`：激活接口（需已是 admin + 口令正确→分配 super_admin 角色）、管理员信息列表（邮箱脱敏）、超管口令增删改查；权限用 `require_super_admin`
- 前端 `SuperAdminPage.vue`：非超管显示激活表单，超管显示 3 个 tab（管理员信息/超管口令/SQUAD闪电谈文章）
- 验证：错误口令 403、普通用户 403、激活成功、口令增删改、邮箱脱敏 q***d@163.com 全通过

### 4. 视频图标修复
- 根因：`bili_videos.cover_url` 为空
- `api/video.py`：cover_url 为空时自动调 B站 API（`x/web-interface/view`）获取封面，Redis 缓存 24h；已验证真实拿到封面
- `GuidePage.vue`：img 加 `referrerpolicy="no-referrer"`（防 B站防盗链）+ onerror 回退 🎬

### 5. 封禁系统
- `users` 表加 `banned_until`；`api/deps.py` `get_current_user` 封禁期内抛 403（一处拦截覆盖所有身份验证操作）；`get_current_user_optional` 封禁视为未登录；登录/refresh 也检查
- `api/admin.py` 新增 `PUT /admin/users/{id}/ban`（duration_hours 自定义，0 解封）
- 前端 `UserManage.vue`：常用时长下拉 + 自定义小时、封禁状态展示、解封

### 6. 资料审核系统（头像/昵称/个性签名）
- `users` 表加 pending_avatar/pending_nickname/pending_bio + 提交时间
- `api/users.py`：修改昵称/签名/头像只写入 pending；`api/audit.py`：待审核列表 + 通过/拒绝（通过才写入正式字段并展示）
- 前端：`AuditManage.vue` 审核页；`ProfileSettings.vue` 显示"审核中"标签和提示

### 7. 文章管理 → 超管后台，仅 SQUAD闪电谈
- "SQUAD闪电谈" = DeveloperPage 展示的 `category=developer` 文章
- `api/cms.py`：admin 文章接口全部改 `require_super_admin`，list/create/update/delete 强制只操作 developer 分类（create 固定分类、update 禁改分类）
- 前端：AdminLayout 移除文章管理菜单，超管页新增"📝 SQUAD闪电谈文章"tab（内嵌 CRUD 对话框）

### 技术备注
- `create_all` 不会给已存在表加列：users 表新列需手动 ALTER（已执行）
- badges 表已有数据时 seed 跳过：12 个新勋章手动 INSERT（已执行）
- 后端是用户本地 `uvicorn --reload`（miniconda FLASH_END_PRO 环境，8000 端口），改代码自动热重载；测试时发现表缺列导致 500，ALTER 后恢复
- 测试用临时账号已全部清理；管理员口令池 use_count 3/5（"闪电的战术大队"）
- 验证邮件真实发到 qingsdao_ddvd@163.com（登录验证码，5 分钟有效）
