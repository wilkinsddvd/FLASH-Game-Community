# 2026-08-02

## FLASH 口令系统改造（需求文档 v1.2）
- 新文档要点：口令池可增加/删除、不可修改；每口令限用 5 次；初始口令「闪电的战术大队」
- 后端：admin_passphrases 表加 use_count/is_builtin 列；verify 遍历口令池、命中即 +1、用满失效；API 改为 GET 列表 / POST 新增 / DELETE 删除（内置口令和最后一个不可删）
- 前端：PassphraseManage.vue 重写为口令池列表+新增表单；api.js 换成 list/add/delete
- 顺带修复：users.uid NOT NULL 无默认值导致所有注册 500（v2 UID 体系的半成品）→ 重建 core/uid.py（8~10位随机+查重）、User 模型补 uid 列、4 处 User() 创建点赋值
- 测试：10 项接口用例全部通过（含防爆破 5 次锁定 30 分钟、口令用满第 6 次拒绝），测试数据已清理，内置口令 use_count 已重置为 0
- 备注：需求文档.md 已同步 v1.2 口令规则；数据库无需其他迁移

## FLASH 站内信系统（需求文档 v1.2 第二部分）
- 恢复被删源码（pyc 还原）：model/message.py（消息类型 system_notice/private_message/interaction）、schemas/message.py
- 新增 api/messages 路由：GET 列表(分页/类型/未读过滤)、unread-count、POST 私信(支持 receiver_id/username)、PUT read/read-all、DELETE、POST system 广播(需 cms:manage)
- 互动通知接入：回复/点赞→通知帖主（点赞同时维护作者 like_received）；新增关注体系（model/follow.py + api/users.py：GET /users/{uid} 空间页、POST/DELETE follow，带 is_following）
- 顺带补齐：User 模型补全 v2 用户信息字段（与 DB 对齐）；UserInfo 加 uid；前端已调用但后端缺失的 GET /users/{uid}、like/favorite（其实 like/fav 接口已存在）
- 前端：MessagePage 挂 /messages 路由 + 30s 短轮询 + 发私信弹窗；App.vue 站内信入口+未读角标(60s 轮询)；SpacePage 关注/私信按钮；admin 新增系统通知页 NoticeManage
- 测试：16 项端到端全过（私信/点赞/回复/关注通知、系统广播、已读、删除、鉴权），测试数据已清理
- 已知缺口：SpacePage 头像/背景上传接口（/users/me/avatar、/users/me/space-cover）后端未实现，属于 v2 用户信息模块，待做

## FLASH 六项 Bug 修复
1. 管理后台按钮：isAdmin 改为 computed(auth.user.role)，/auth/me 返回 role；登录即显示无需刷新
2. 管理后台：角色管理去掉分配权限；权限管理表格去编码/操作标识列，新建权限只填名称(后端自动生成 code/action)；板块/文章新建改跳转独立页面(新增 SectionCreatePage.vue、ArticleFormPage.vue，路由 /admin/sections/create、/admin/articles/create、/admin/articles/:id/edit)
3. 个人空间上传：后端新增 POST /users/me/avatar、/users/me/space-cover（JPG/PNG、2MB/5MB、自动删旧图），main.py 挂载 /uploads 静态目录
4. 个人设置：补齐 GET/PUT /users/me（昵称90天限1次、性别/生日不可改）、PUT /users/me/username（验密码、180天限1次）、GET /users/{uid}/level（等级体系 Lv1-6）；路由 /settings + 导航入口
5/6. 普通/管理员注册页隐私条款：加勾选+《隐私政策》弹窗（管理员页也补上，未勾选禁用注册）
- 测试 12 项全过；测试数据已清理；uploads 目录保留
